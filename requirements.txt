# Dipendenze Python del progetto.
# Installazione:  python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
# (scripts/setup.sh lo fa da solo)
pytest>=8
anthropic>=1.0
numpy>=2

# Facoltativi, solo per alcune ricerche in ricerca/ e scripts/:
#   numba      ricerca/a105020_indici.py     (compilazione JIT)
#   networkx   ricerca/residui_d27.py
#   sympy      scripts/caccia_programmi.py
#   highspy    la ricerca sui codici, quando usa la programmazione lineare
# Non servono per i test ne' per il verificatore.
