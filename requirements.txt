# The project's Python dependencies.
# Install with:  python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
# (scripts/setup.sh does it for you)
pytest>=8
anthropic>=1.0
numpy>=2

# Optional, only for some of the searches in search/ and scripts/:
#   numba      search/a105020_indices.py    (JIT compilation)
#   networkx   the residue-cover search
#   sympy      scripts/hunt_programs.py
#   highspy    the code search, when it uses integer programming
# None of these is needed for the tests or for the verifier.
